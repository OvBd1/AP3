import React, { useState } from 'react';
import './home.css';

const Home = () => {
    const [message, setMessage] = useState("");

    return (
        <header>
            <h1>GSB</h1>
        </header>
    );
};

export default Home;
